// TODO: Esta sección se habilita por cliente cuando aplique.
// En V1 del starter no leemos testimonials de site.config.ts.
// Para activarla: 1) editar site.config.schema.ts para aceptar `testimonials[]`,
// 2) poblar `siteConfig.testimonials` con datos reales verificados, 3) reemplazar este shell.
export default function Testimonials() {
  return null;
}