// TODO: Esta sección se habilita por cliente cuando aplique.
// En V1 del starter no leemos pricing de site.config.ts.
// Para activarla: 1) editar site.config.schema.ts para aceptar `pricing[]`,
// 2) poblar `siteConfig.pricing` con datos reales, 3) reemplazar este shell.
export default function Pricing() {
  return null;
}